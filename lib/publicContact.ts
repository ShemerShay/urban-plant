/**
 * Public-facing contact details (accessibility statement, recovery CTAs).
 * Keep in sync with WhatsApp defaults and purchase email reply-to.
 */

/** Same address as PURCHASE_EMAIL_REPLY_TO in purchaseEmail.ts (server-only module). */
export const PUBLIC_CONTACT_EMAIL = "beherha@gmail.com";

/** Display form of the default WhatsApp business line (054-660-5603). */
export const PUBLIC_WHATSAPP_DISPLAY = "054-660-5603";
